---
'maintainers.space': minor
---

Adds a file tree to the pull request Files changed tab. Each changed file appears in a
sidebar with its status and diff stat; diffs render lazily (only the files you reach or
pick), and jumping to a file from the tree is instant once it is loaded.
