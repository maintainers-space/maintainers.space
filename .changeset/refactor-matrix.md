---
'maintainers.space': minor
---

Adds repository-scoped access tokens: when an organization blocks third-party OAuth apps, you can save a fine-grained token for that repository and comments, reviews and reactions there use it. Forge pages now also hide features a forge doesn't support instead of failing to load.
